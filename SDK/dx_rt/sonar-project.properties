sonar.projectKey=deepx_dx_rt_AZZa5znCgP_2ECN7V4UZ
sonar.sourceEncoding=UTF-8
sonar.sources=examples,lib,python_package,test
sonar.cfamily.build-wrapper-output=bw-output

# C/C++ specific settings
sonar.cfamily.build-wrapper-output=bw-output

# Coverage settings
sonar.coverageReportPaths=coverage.xml

# Exclusions (optional - adjust based on your needs)
sonar.exclusions=**/3rdparty/**,**/build*/**,**/*.pb.h,**/*.pb.cc

# Test exclusions (테스트 대상이 아닌 파일들)
sonar.test.exclusions=**/examples/**,**/python_package/**,**/test/**,**/*test*/**,**/3rdparty/**,**/build*/**,**/*.pb.h,**/*.pb.cc

# Coverage exclusions (커버리지 측정 시 제외할 파일들)
sonar.coverage.exclusions=**/examples/**,**/python_package/**,**/test/**,**/*test*/**,**/3rdparty/**,**/build*/**,**/*.pb.h,**/*.pb.cc

# Additional C++ settings (optional)
sonar.cfamily.threads=4
sonar.cfamily.cache.enabled=true
sonar.cfamily.cache.path=/__w/dx_rt/dx_rt/.sonar/cache

sonar.coverage.branch.enabled=true
